// Central chart palette derived from design tokens (tokens.css)

export const chartPalette = [
  'var(--canopy)',
  'var(--leaf)',
  'var(--jacaranda)',
  'var(--flame)',
  'var(--moss)',
  'var(--clay)',
  'var(--sand)',
  'var(--slate)'
]

export const stackedPalette = [
  'var(--leaf)',
  'var(--jacaranda)',
  'var(--flame)',
  'var(--moss)'
]

export const heatmapGradient = {
  min: 'rgb(var(--sky-rgb) / 0.4)',
  mid: 'rgb(var(--leaf-rgb) / 0.8)',
  max: 'rgb(var(--flame-rgb) / 0.8)',
}
